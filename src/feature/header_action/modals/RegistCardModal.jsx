import React from "react";

export const RegistCardModal = () => {

  return ReactDOM.createPortal (
    <div>

    </div>,
    document.getElementById("root")
  );

};

export default RegistCardModal
